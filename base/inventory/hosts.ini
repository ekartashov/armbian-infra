# hosts.ini — MAC→hostname registry for provisioned boards
# Managed by the provision-base.yml playbook (append-only on success)
#
# Format: MAC  hostname  status
# Status: provisioned | failed | decommissioned
#
# This file is NOT a standard Ansible inventory — it is a registry.
# Ansible targets are passed ad-hoc: ansible-playbook -i "10.42.0.X," ...
#
# ── Provisioned hosts ───────────────────────────────────────────────────────
