# Downloaded images (large binaries)
images/*/Armbian_*.img.xz
images/*/Armbian_*.img.xz.sha
images/*/Armbian_*.img.xz.asc

# Patched images
images/patched/*.img.xz
images/patched/*.img

# Cached catalog
cache/

# Secrets (keys, password hashes)
secrets/

# Lock files — tracked by default (reproducibility).
# Uncomment to ignore:
# images/*/.pull-lock
# images/patched/*.patch-lock

# Transient flock(1) lock used by allocate-hostname.sh — never committed
base/inventory/hosts.ini.lock

# Repo export
repomix-output.xml
